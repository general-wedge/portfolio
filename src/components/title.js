import React from 'react'
import styles from '../styles/title.module.scss'

const Title = () => (
  <div className={styles.titleWrapper}>
    <h1 className={styles.title}>austin howlett</h1>
  </div>
)

export default Title